//Este archivo funciona para importar en un solo import toda la carpeta de resolvers.
import createToken from './create';
import verifyToken from './verify';

export default {
    createToken,
    verifyToken
}